# Defeitos achados pela bateria

Um defeito por linha, na ordem em que foram achados. "Travado por" é o teste que
quebra se o defeito voltar.

| Data | Cenário | Defeito | Causa | Commit | Travado por |
|---|---|---|---|---|---|
| 14/09/2026 | (antes da bateria) | reauditar o mesmo memorial mostrava o parecer antigo | id da auditoria por documento | a486a53 | scripts/test-auditoria-da-proposta.ts, jornada a3 |
| 14/09/2026 | (antes da bateria) | contagem de auditoria incompleta parecia o total | nenhuma tela lia `passadas_incompletas` junto do número | 983ef51 | scripts/test-auditoria-incompleta.ts, jornada a1 |
| 14/09/2026 | a3 | reauditar o memorial: rodada 2 na tela, só a rodada 1 no IndexedDB (F5 sem /api/audits voltava à rodada 1); o mesmo desenho perdia o título do dossiê e podia regravar o bilhete residual | a gravação imediata (flushPersist) lia o snapshotRef antes do commit do React e nada gravava o estado comitado depois; agora ela pede outra gravação amarrada à geração que vai no estado junto com a mudança, cumprida só pelo commit que a carrega | 13ce603, ae3f61a, e6a7833 | scripts/test-agenda-de-gravacao.ts + a3 (disco lido 300ms depois da rodada 2; F5 sem a rede de recuperação; bilhete fora do disco) |
| 15/09/2026 | c6 | F5 com outra conversa como última e um bilhete residual numa conversa com parecer pronto: a retomada trocava sozinha para ela, a anterior era gravada com o memorial e o `createdAt` da aberta, `nexo:ultima-conversa` voltava para a anterior e a aberta ficava com o bilhete no disco; no F5 direto, um registro fantasma "Nova conversa" com o memorial da aberta (2 de 3 corridas) | entre as escritas à mão de `selectConversation` no snapshot e o commit que traz o id novo, qualquer gravação imediata (o palco limpando o bilhete dentro do commit da troca; o flush da segunda abertura da mesma conversa) lia o id antigo com campos da nova; agora a troca é marcada e, até o commit da nova, só fica um pedido para ela | a602c5a | scripts/test-agenda-de-gravacao.ts (troca: effect de filho no commit da troca; duas aberturas seguidas; debounce na janela) + c6 |
| 15/09/2026 | a5 | auditar de novo o mesmo memorial: a recusa "documento idêntico" aparecia, mas cada tentativa deixava uma linha "Audit" em PROCESSING para sempre e o evento "Auditoria criada" no histórico do projeto | `createPendingAudit` cria a linha antes da checagem de idêntico, e o `return` da recusa não passa pelo `catch`; agora a recusa apaga a linha PROCESSING e o evento daquele id | bd09afa | scripts/test-auditoria-recusada.ts + a5 |
| 15/09/2026 | c5 | abrir outra conversa com a auditoria em voo: a conversa aberta ficava marcada por ~1s e a tela voltava sozinha para a que auditava | a retomada da auditoria em voo (`retomouRef` em `NexoWorkspace.tsx`), feita para o F5, só se dava por decidida quando retomava; com a conversa que audita já aberta na carga ela ficava armada e tratava o primeiro clique para longe dela como a carga; agora decide uma vez, na primeira lista de conversas que chega. A jornada c5 da Tarefa 12 esperava 800ms fixos para caber no puxão; agora espera a conversa aberta virar B | 020e1a4 | scripts/test-retomada-da-auditoria.ts + c5 (B continua aberta alguns segundos depois) |
| 15/09/2026 | c5 | abrir outra conversa durante a auditoria: quando a resposta chegava, o parecer de A era gravado em B (disco e canvas de B) e o bilhete limpo era o de B | `saveResult` e `marcarAuditoriaPendente(null)` miram a conversa aberta no instante da chegada; agora o cartão lembra a conversa de origem e, com outra aberta, não grava nem limpa — A recebe o parecer pela reconexão ao ser reaberta. A verificação de tela da c5 contava só "Ver o parecer" (que mora no chat de A) e passava com o parecer no canvas de B; agora conta também o nome do memorial de A | eb2c841 | scripts/test-destino-do-parecer.ts + c5 |
| 15/09/2026 | c3 | uma aba parada desde antes da auditoria gravava depois e apagava o parecer da outra aba — no servidor E no IndexedDB que as duas dividem (medido na Tarefa 14: `pareceres=0` nos dois, versão do servidor trocada, e a rota respondia 200 a uma base velha); sem aviso nenhum na tela | o `PUT` só descartava gravação com hora mais velha, e a hora é a da GRAVAÇÃO, não a do conteúdo; e o disco era gravado sem conferir nada. Decidido pelo Matheus: cada aba grava contra a versão que leu; o disco (conferido na hora de executar, numa fila) e o servidor (409, com a base e as versões próprias ainda sem confirmação) recusam a desatualizada, e a tela oferece recarregar. Na revisão: quando quem gravou antes é OUTRA MÁQUINA, o disco desta não tem nada mais novo e a gravação parada pousava nele antes do 409, e um F5 reabria dela e apagava a outra máquina — agora o 409 desce a cópia do servidor para o disco (pela fila, com época) e uma marca local faz a reabertura ler do servidor até ela pousar. Na segunda revisão: a abertura do F5 vinha sempre do disco (antes de a lista do servidor chegar), e uma gravação desta aba que chegou ao servidor e não ao disco dava 409 falso — a descida apagava as edições; agora a abertura da carga espera a lista do servidor e abre da cópia mais nova, e base não conferida trava sem descer nada. No caminho, a regressão achou e fechou: 409 falso contra a própria aba com duas gravações no mesmo milissegundo (a3), e a ida ao servidor atrasada pela fila sendo cortada quando a aba fechava (a1 → a2) | 2f16ed6, 3793bcc, 3a41e92 | scripts/test-fila-de-gravacao.ts + scripts/test-nexo-conversa-remota.ts (gravacaoDesatualizada) + c3 |
| 15/09/2026 | c5 | F5 de volta para uma conversa com bilhete de auditoria no disco: a conversa abria duas vezes, e o chat remontava ~0,5s depois de o parecer aparecer, com a rolagem de volta ao topo (a c5 falhava em "A mostra o parecer, visível de verdade": Playwright rolava, e o chat era trocado antes da medida) | a restauração da última conversa e a retomada da auditoria em voo abriam a MESMA conversa: a retomada decidia na primeira lista com a conversa nova ainda aberta (medido: `restaura A` 598ms, `retoma A aberta=<nova>` 626ms, `role=log` trocado em 1749ms e 2166ms); agora ela não reabre a conversa que a carga já está abrindo. Não é da Tarefa 15: a mesma falha aparecia no cliente de 1b6bf8a, e na corrida 1 da Tarefa 13 | b69371b | scripts/test-retomada-da-auditoria.ts + c5 |
| 15/09/2026 | v2 | prancha com carimbo sem texto: o leitor respondia com todos os campos nulos e a folha passava por lida — chip em branco, nenhuma ressalva na conversa; a revisão achou o mesmo objeto vazio guardado no CACHE como "lido" (`selo-cache.ts`), e a folha vazia, marcada com `error`, nunca entrava no cache — repagava o modelo a cada reanexação | "não lida" era só `extraction: null` (a chamada que falhou); a resposta vazia é um objeto. Agora `extractSeloFromPage`/`extractSeloFromImage` devolvem a leitura vazia como não lida, com o motivo. `VERSAO_DO_LEITOR` subiu (2→3) e `reidratar` desarma qualquer leitura vazia que ainda chegue do disco, de qualquer versão; `leituraCompleta` passou a distinguir a leitura vazia (`vazia: true` — guarda, não repaga o modelo) da falha transitória (`error` sem `vazia` — continua fora do cache) | a8825cb, 85d4c24 | scripts/test-leitura-do-selo-vazia.ts + scripts/test-nexo-selo-cache.ts + v2 |
| 15/09/2026 | x1 | sessão caída durante a auditoria: depois do F5 o palco perguntava pela análise de 5 em 5s para sempre, sem a faixa "Sessão expirada"; na largada, o 401 virava "conexão caiu" com bilhete eterno | `consultarAuditoria` tratava 401 como banco fora do ar, e nem ele nem `runMemorialAudit` chamavam `conferirSessao`; agora os dois chamam, a largada lança `SessaoExpiradaNaAuditoria` e a reconexão para em `sem-sessao` guardando o bilhete | 85d0748 | scripts/test-nexo-audit-desconexao.ts + x1 |
| 15/09/2026 | c1 (revisão final) | soltar a REVISÃO do memorial com o mesmo nome era ignorado como repetido: nenhuma leitura nova, e a auditoria rodaria na versão velha | a deduplicação da c1 comparava só o nome, e dois PDFs diferentes com o mesmo nome são a regra na revisão (`use-delta-do-memorial.ts`); agora repetido é mesmo nome, tamanho e sha-256, e mesmo nome com bytes novos troca o memorial com "Troquei o memorial pela versão nova" (R22) | bc9cfc6 | scripts/test-memorial-repetido.ts + c1 (revisão com o mesmo nome) |
| 15/09/2026 | x1 (revisão final) | membro desativado ou removido durante a auditoria: a reconexão perguntava para sempre; na largada virava "a conexão caiu" com bilhete guardado | `consultarAuditoria` tratava todo não-ok fora 401/404 como banco fora do ar; na largada o 4xx caía em `lerFluxo`, e no modo de fluxo (HTTP sempre 200) a recusa do portão chegava sem status; agora 403 é `sem-acesso` (para e guarda o bilhete), 400 é irrecuperável, todo 4xx fora 408/409/429 fecha o ciclo e o `event: error` leva o status (R21) | 52467e2 | scripts/test-nexo-audit-desconexao.ts |
| 15/09/2026 | c3 (revisão final) | a aba travada pela conversa mudada em outra aba ainda auditava, conversava com o agente e gerava LD: a auditoria era paga e o parecer nunca registrado | a trava só desligava a gravação (`fila-de-gravacao.ts`); nada que gasta modelo lia `conflitoDeVersao`; agora o store expõe `podeGastar` e botões de confirmar, `confirm` da auditoria e envio do chat recusam com a frase da faixa | 1984678 | scripts/test-aba-travada.ts + c3 (envio e Auditar travados na aba parada) |
| 15/09/2026 | c3 (revisão final) | duas gravações da mesma conversa quase juntas no servidor: as duas liam a mesma versão e a segunda apagava a primeira (janela de milissegundos, sem caso medido na tela) | o PUT lia a versão e depois fazia `upsert`, sem atomicidade; agora é compare-and-set (`updateMany` com o `updatedAt` lido; P2002 relê) e cada desencontro reaplica as regras. Uma releitura só deu 409 falso na rajada de três gravações da própria aba (medido na c3), por isso há tentativas até `TENTATIVAS_DE_GRAVACAO` | 4834605 | scripts/test-nexo-conversa-remota.ts (gravarComVersao) + c3 |
| 15/09/2026 | c5/a4 (revisão final) | duas aberturas de conversa soltas pela mesma espera (F5 restaurando a última, retomada da auditoria, clique na barra): trocava por último a que terminasse por último, e o F5 podia desfazer um clique | `selectConversation` espera commit, fila, lista do servidor (até 4s), disco e rede sem conferir se ainda é a abertura mais recente; o sintoma só era silenciado no `motion.ts`; agora a última abertura vence (`ultima-abertura.ts`) | ae29186 | scripts/test-ultima-abertura.ts + a4, c5, c6 |
| 15/09/2026 | v2 (revisão final) | carimbo parcial (só cliente e data legíveis) passava por "não lido", perdia cliente, obra e data, e ia para o cache como vazio | `leituraDoSeloVazia` olhava 6 dos 13 campos, mais estreita que o desenho ("tudo nulo"); agora exige os 13 nulos, e `VERSAO_DO_LEITOR` subiu 3→4 para o cache não devolver as parciais guardadas como vazias | b1b8b38 | scripts/test-leitura-do-selo-vazia.ts + v2 |
| 15/09/2026 | c3 (depois da segunda rodada) | o primeiro gesto pago de uma aba parada (turno do agente, auditoria, conferência do volume, geração pelo plano) ainda saía: a trava só acendia com a gravação recusada, e o modelo rodava antes (medido na c3 com a conferência desligada: POSTs /api/nexo/agent=1) | nada perguntava a versão ao servidor antes de gastar; agora `conferirAntesDeGastar` lê o `updatedAt` da lista e aplica `gravacaoDesatualizada` com as próprias, travando como um 409 (fora do alcance, gasta sem conferir a base) | 0957aa0 | scripts/test-conferir-antes-de-gastar.ts, jornada c3 |
| 15/09/2026 | c3 (depois da segunda rodada) | "Regenerar" de LD, capa e separatriz ativo na aba travada: o documento era gerado e descartado calado; "Auditar de novo" abria rodada numa conversa que a aba não grava | só volume e plano liam `podeGastar` no Regenerar; agora os três `confirm` recusam, o `ResultLinks` diz o motivo e a âncora esconde "Auditar de novo" | 9a1774c | scripts/test-regerar-com-trava.ts |
| 15/09/2026 | c7 | trava com base não conferida (marca "manter"): a faixa dizia "Esta conversa mudou em outra aba" — falso ali — e "Recarregar a conversa" trocava o disco pela cópia do servidor, apagando edições que só existiam nesta máquina (contra a faixa antiga: "sem conferir=0 outra aba=1") | a trava não guardava a origem; agora distingue outra-aba de sem-conferir, a faixa e os motivos dos botões dizem o que se sabe, e "Recarregar do servidor" pede confirmação quando o disco tem outra versão ("Continuar travada" não mexe em nada) | 9d7cde8 | scripts/test-aba-travada.ts, jornada c7 |
| 15/09/2026 | (suspeitas da onda final) | "Nova conversa" logo depois de uma mudança no mesmo tick perdia a mudança; e a abertura do F5 em voo trocava por cima de "Nova conversa" (medido no modelo) | `newConversation` dava o flush e trocava na mesma volta, e não pegava a vez em `aberturas`; agora `comecarNovaConversa` pega a vez e espera o commit quando a agenda tem mudança sem commit | ddb9a2f | scripts/test-nova-conversa.ts |
| 15/09/2026 | c5 (suspeita da segunda rodada) | resposta da auditoria de A na janela de milissegundos da troca para B: o parecer de A ia para B e o bilhete de B era limpo (medido no modelo) | `conversaAberta()` lia o snapshot, ainda com o id de A; a origem capturada na largada não bastava. Agora devolve o destino da troca | c381c45 | scripts/test-agenda-de-gravacao.ts |
| 15/09/2026 | a8 | aba restaurada (F5, ou segunda aba abrindo a última conversa): o "Auditar" do cartão de auditoria nascia cinza e ficava assim, e o cartão dizia "arraste o PDF do memorial" com o memorial retido no disco — a conversa restaurada não auditava mais o documento que leu (medido na a8: `Auditar|disabled` por 20s nas duas portas; a c3 contornava provando a recusa por envio no chat) | `selectConv` zerava `memorialFile` (e o dossiê) dentro do callback da view transition, que o navegador chama quadros depois; `recuperarMemorial` voltava do IndexedDB antes (pedido 931ms, memorial 997ms, callback 1018ms) e a limpeza chegava por último. Agora `runShellTransition` devolve uma promessa que resolve depois de `apply`, e `selectConv` espera por ela antes de restaurar | 71151a7 | scripts/test-transicao-do-shell.ts, jornada a8, jornada c3 |
| 15/09/2026 | c7 (revisão da frente A) | trava sem conferir furada pela barra: depois de "Continuar travada", clicar de novo na conversa (ou sair e voltar) pousava a cópia do servidor no disco sem confirmação e destravava — as edições só desta máquina sumiam (medido na c7 contra o código anterior: `updatedAt` do servidor e 1 mensagem no disco logo após o clique) | com a trava na memória, `decidirAbertura` lia do servidor primeiro, qualquer que fosse a origem; agora a trava sem conferir (a fila sabe, via `baseConferida`) só lê do servidor com `recargaConfirmada`, que só a faixa passa | 95a0198 | scripts/test-abertura-da-conversa.ts, jornada c7 |
| 15/09/2026 | (revisão da frente A) | "Nova conversa" pela tela (dentro de `flushSync`) e abrir outra conversa por clique, com mudança pendente de uma continuação assíncrona: a mudança ainda se perdia (medido no modelo de duas faixas) | o flush pedia a geração na faixa síncrona, o commit síncrono a trazia sem a mudança da faixa padrão e a espera resolvia cedo; agora espera o commit da geração que entrou junto com a mudança (`geracaoSemCommit`) e só então dá o flush | 95a0198 | scripts/test-nova-conversa.ts |
| 15/09/2026 | c3 (última onda da frente A) | aba travada ainda pagava leitura no drop: soltar uma prancha chamava o leitor de carimbo (medido na c3 com a trava da leitura desligada: POSTs /api/ld/extract-stamp=1), e o delta do memorial perguntava ao servidor; o que voltasse era descartado pela fila | nada no drop lia `podeGastar`; agora `lerPranchas`, a correção memorial→prancha e `useDeltaDoMemorial` consultam a trava acesa (`recusaDeLeituraPaga`, sem ida à rede) e a conversa diz por que não leu | f8a8aa0 | scripts/test-aba-travada.ts, jornada c3 |
| 15/09/2026 | (última onda da frente A) | trava provada (outra aba) recarregada pela faixa com a rede fora: a reabertura seguinte pela barra ficava no disco sem tentar o servidor e a faixa passava a dizer "não deu para confirmar" (sem caso medido na tela) | a base volta "não conferida" na recarga offline, e só isso decidia a origem; agora a marca "descer" e a origem lembrada nesta aba mantêm a trava provada (`travaSemConferirAoAbrir`) | f8a8aa0 | scripts/test-aba-travada.ts |
| 15/09/2026 | (última onda da frente A) | recusa de A entre o começo da troca para B e o commit dela acendia a faixa em B (sem caso medido) | `marcarConflito` comparava com o id do snapshot, ainda o de A; agora usa `agenda.abertaAgora`, a mesma regra de `conversaAberta()` | f8a8aa0 | scripts/test-agenda-de-gravacao.ts |
| 15/09/2026 | (modelo) | mudança de continuação assíncrona (faixa padrão) sem commit e, no meio, o flush de outro gesto num clique (faixa síncrona): "Nova conversa" e a abertura soltavam a espera antes do commit da mudança e ela se perdia na troca; e, mesmo sem troca, o pedido pós-commit desse flush era cumprido sem a mudança e nada mais a gravava (medido no modelo de duas faixas) | a agenda guardava só a maior geração comitada e comparava por `>=`, e o commit síncrono com g+1 passava por g, ainda na faixa padrão; agora o estado do React guarda a LISTA das gerações (`juntarGeracao`, na mesma volta da mudança), o commit entrega a lista e a espera e o pedido só resolvem quando nenhuma geração até a deles falta chegar a um commit | d2e19a9 | scripts/test-nova-conversa.ts (flush de outro gesto na faixa síncrona) |
| 15/09/2026 | c3 (depois da última onda da frente A) | na aba travada, corrigir o chip de um memorial para prancha trocava o chip para "prancha" com a leitura recusada e o memorial ainda retido (medido na c3: antes prancha=1 memorial=1, depois prancha=0 memorial=2) | em `definirPapelAnexo` a recusa da leitura paga vinha depois de `setAttachments`; agora sai antes de qualquer mudança | 1d6e2b6 | jornada c3 |
| 15/09/2026 | c3 | aba travada guardava o memorial solto (ou a prancha corrigida para memorial) por cima do memorial da outra aba: o IndexedDB é o mesmo, e o F5 da aba em dia voltava com o arquivo errado; ainda chamava o classify (medido na c3 contra o código anterior: blob md_bateria_sem_codigo.pdf, 2588 bytes, no lugar do da aba 1, 2620; 1 POST a /api/nexo/classify) | `salvarMemorial` escreve direto no IndexedDB, fora da fila que a trava fecha; agora `readSelos` e `definirPapelAnexo` recusam o memorial na aba travada, com o porquê na conversa | 431e0f7 | jornada c3 |
| 15/09/2026 | x3 | acesso suspenso (403) no meio da retomada de uma auditoria: a reconexão parava de perguntar e guardava o bilhete, mas o palco mostrava "análise em curso" para sempre, sem motivo e sem saída (medido na x3 contra o código anterior: motivo=0, retomada=1, sem botão) | o palco escolhe a espera sempre que há bilhete, antes de olhar a falha; agora `vistaDaReconexao` tira a espera de cena para o bilhete bloqueado, mostra o motivo e oferece "Descartar esta espera" | 85b5b20 | scripts/test-vista-da-reconexao.ts + x3 |

## Suspeitas abertas

Nenhuma.
