# Dependencies
node_modules/

# Next.js
.next/
out/
next-env.d.ts

# Build outputs
#
# ANCORADAS NA RAIZ (`/build/`, e nao `build/`), porque a forma solta casa com
# QUALQUER diretorio de mesmo nome em QUALQUER profundidade -- e foi o que
# aconteceu: `build/` engoliu `app/api/volume/build/`, uma ROTA. O arquivo nunca
# entrou no git, e por isso nunca chegou na Render: `/api/volume/build`
# respondia 404 em producao enquanto a irma `/api/volume/analyze` respondia 405.
# Montar volume estava quebrado no ar, e ninguem tinha como ver isso no diff.
/dist/
/build/
/coverage/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# TypeScript
*.tsbuildinfo

# Temporary files
tmp/
temp/
.cache/

# Local artifacts
#
# `/uploads/` ancorada pelo mesmo motivo que as de build: solta, ela engolia
# `app/api/projects/[id]/uploads/`, que tambem nunca chegou em producao.
/uploads/
backups/
*.local
.nexodoc-learnings.json
data/nexodoc-learnings.json

# Amostras CONFIDENCIAIS de projetos — NUNCA versionar (manter so o README)
/docs/samples/*
!/docs/samples/README.md
.superpowers/

# Config/skills de assistentes de IA — local de cada maquina, nao do projeto.
# Sem isto um "git add -A" varre milhares de arquivos pra dentro do repo.
.agents/
.augment/
.claude/
.codebuddy/
.codewhale/
.continue/
.cursor/
.factory/
.gemini/
.kilocode/
.kiro/
.opencode/
.qoder/
.roo/
.trae/
.warp/
.windsurf/
# So os prompts de skill; .github segue livre p/ workflows de verdade.
.github/prompts/

# Prints e arquivos de trabalho dos testes de navegador. Ficam de fora porque
# mostram o app carregado com as pranchas de docs/samples, que sao dados reais
# e confidenciais de projeto -- o mesmo motivo daquela pasta ser ignorada.
/scratchpad/

# Mesma razao, mas na RAIZ: a prova de CSS grava uma captura por rodada que
# falha (prova-css-no-login.mjs), e elas mostram a tela logada. Sao descartaveis
# -- o script as refaz a cada investigacao -- e sem esta regra reapareciam em
# todo `git status`, prontas para entrar de carona em commit alheio.
/prova-css-rodada-*.png
# Capturas dos portoes de UI: prova de uma rodada, nao artefato do projeto.
# Regravadas a cada execucao e pesadas -- versiona-las so engorda o historico.
/docs/provas/

# Estado de execucao da ferramenta de critica: PID, porta e um TOKEN, reescritos
# a cada sessao. Nao e codigo nem configuracao -- e ruido que aparecia em todo
# `git status` e entrava de carona em commit alheio. Token, ainda que so de
# localhost, nao tem por que morar no historico.
/.impeccable/live/

# A entrega crua da ferramenta de design. A copia canonica vive em
# docs/design-system-v0/, versionada e com cabecalho dizendo que e copia de
# trabalho -- ter as duas seria a verdade duplicada que este repositorio ja
# pagou caro para evitar.
/design-system/
