services:
  - type: web
    name: nexodoc-converter
    env: node
    region: oregon
    plan: free
    branch: main
    rootDir: render-service
    buildCommand: npm install
    startCommand: npm start
