services:
  - type: web
    name: nexodoc-api
    runtime: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start -- -H 0.0.0.0 -p $PORT
    envVars:
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: OPENAI_API_KEY
        sync: false
      - key: OPENAI_MODEL
        value: gpt-5.4-mini
      - key: NEXODOC_ALLOWED_ORIGINS
        sync: false
      - key: NEXODOC_ADMIN_TOKEN
        sync: false
      - key: OPENAI_ADMIN_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: NEXODOC_MOCK_MODE
        value: "false"
      - key: NEXODOC_ALLOW_CLIENT_DEMO
        value: "false"
